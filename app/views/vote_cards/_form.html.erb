<%= form_for(@vote_card) do |f| %>
  <% if @vote_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vote_card.errors.count, "error") %> prohibited this vote_card from being saved:</h2>

      <ul>
      <% @vote_card.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <div id="voter"><%= f.label :name, "Your Name" %>&nbsp;
    <%= f.text_field :name %></div>
  </div>
  <br/>
  <h1>Your Votes</h1>
  <table>
    <tr>
      <td id="superlatives"">
        <div id="vote">&#9731; <%= f.label :best_in_show_id, "Best in Show" %></div>
      </td>
      <td id="superlatives">
        &#9731; <%= f.collection_select :best_in_show_id, @entries, :id, :cookie %>
      <td>
    </tr>
    <tr>
      <td id="superlatives">
        <div id="vote">&#10052; <%= f.label :most_delicious_id, "Most Delicious" %></div>
      </td>
      <td id="superlatives">
        &#10052; <%= f.collection_select :most_delicious_id, @entries, :id, :cookie %>
      <td>
    </tr>
    <tr>
      <td id="superlatives" style="width:250px;">
        <div id="vote"> &#9731; <%= f.label :most_decorative_id, "Most Creative" %></div>
      </td>
      <td id="superlatives" style="width:250px;">
        &#9731; <%= f.collection_select :most_decorative_id, @entries, :id, :cookie %>
      <td>
    </tr>
    <tr>
      <td id="superlatives" style="width:250px;">
        <div id="vote">&#10052; <%= f.label :most_traditional_id, "Kids Choice" %></div>
      </td>
      <td id="superlatives" style="width:250px;">
        &#10052; <%= f.collection_select :most_traditional_id, @entries, :id, :cookie %>
      <td>
    </tr>
  </table>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
